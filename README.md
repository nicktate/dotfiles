# nixos home configuration (macosx)

1. Install nix-os
2. Install darwin-nix
3. Clone repo into ~/.config/nixpkgs
4. 	darwin-rebuild switch -I darwin-config=/Users/nicholastate/.config/nixpkgs/darwin.nix`
